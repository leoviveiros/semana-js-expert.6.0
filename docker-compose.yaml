version: '2.1'
services:
  live-reload:
    container_name: live-reload
    image: node-app
    build: .
    command:
      - npm
      - run
      - live-reload
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "3000:3000"

volumes:
  node_modules: {}